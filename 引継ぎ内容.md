引継ぎ内容

# ゲームダッシュボード プロジェクト概要

## プロジェクトの目的

MMO（主に MapleStory Universe）向けの個人用ダッシュボードを開発する。

ゲーム内で確認しづらい情報や、毎日・毎週行うタスクを一元管理することを目的とする。

コンセプトは「ゲームを改造するツール」ではなく、「ゲーム体験を補助する外部ダッシュボード」である。

---

# 開発方針

* ローカルでの利用を前提として開発する。
* 将来的には無料または低コストで公開できる構成を視野に入れる。
* データベースは使用しない方針。
* 必要になるまでバックエンドを複雑化しない。
* APIから取得できる情報を表示することを基本とし、アプリ側でデータを保持しないステートレス設計を目指す。

---

# 想定機能

## Daily

デイリーコンテンツ管理

* キャラクターごとのデイリー管理
* チェック状態管理
* リセット対応

---

## Boss

ボス消化管理

* キャラクターごとの討伐状況
* ボス一覧
* 消化状況表示

---

## Weekly Rewards

先週のウィークリーボス報酬閲覧

目的

ゲーム内ではキャラクターごとにしか確認できない先週の報酬を、アカウント単位で一覧表示する。

### データ取得

URLからウォレットIDを取得。

例

```
/weeklyRewards?wallet=xxxxxxxx
```

処理

1. ウォレットID取得
2. アカウント取得
3. キャラクター一覧取得
4. 各キャラクターの報酬取得
5. データ統合
6. 表示

取得はAPIのレート制限を考慮し、同時実行数を制限したキュー方式で実行する。

全件並列ではなく、同時実行数（例：5～10件）を設定可能にする。

APIエラー時は指数バックオフ付きリトライを行う。

---

# 将来的な追加予定（今回は実装対象外）

* JSONエクスポート
* JSONインポート
* 長期履歴管理
* 確率表示
* 期待値分析
* レア度分析
* 統計ページ
* 幸運度・スコア表示
* ゲームのUIに合わせた画面表示モード

これらは将来の拡張機能とし、現時点では設計のみ考慮する。

---

# データ保持方針

基本方針

ステートレス

* DBなし
* LocalStorageなし
* Cookieなし
* API取得結果のみで画面を構築

ユーザー自身が保存したい場合のみ、将来的にJSONエクスポート機能を提供する。

---

# UI方針

ゲームのUIデザインには寄せず、モダンで扱いやすい設計にする。
カードやラベルが複数並ぶなどノイズの多いデザインは避けグリッド表示を優先する。

現段階ではUIデザインより機能設計を優先する。

---

# 技術構成

## フロントエンド

* HTML
* CSS
* JavaScript
* Vite

---

## API通信

標準の fetch を利用する。

Axiosは使用しない。

---

## バックエンド

公開時にAPI中継が必要な場合は Cloudflare Workers を使用予定。

責務は

ブラウザ
↓
Worker
↓
公式API

のみ。

ゲームロジックは持たせない。

---

## 公開方法

第一候補

* Cloudflare Pages
* Cloudflare Workers

または

* GitHub Pages
* Cloudflare Workers

ローカルではViteの開発サーバーを利用する。

---

# 開発ツール

採用

* Vite
* Git
* GitHub
* TypeScript

将来必要なら追加する


---

# API設計方針

画面から直接公式APIを呼ばない。

画面は常に自分が提供するAPIだけを見る。

つまり

UI

↓

自前API

↓

公式API

という構造を維持する。

将来的にFastAPIや別のバックエンドへ変更しても、フロント側を変更しない構成を目指す。

---

# ディレクトリ構成

```
game-dashboard/

public/

src/
    api/
    components/
    pages/
    services/
    state/
    styles/
    utils/

worker/

mock/

docs/

package.json
vite.config.js
README.md
```

各フォルダの役割

api/
API通信のみ

components/
UI部品

pages/
画面

services/
データ取得・加工

state/
共有状態

utils/
共通関数

styles/
CSS

worker/
Cloudflare Worker

mock/
APIモック

docs/
設計書

---

# 開発方針

* まずはモックデータでUIを作る。
* API完成後に差し替える。
* 機能単位で実装を進める。
* 必要になった時点で技術を追加する。
* 過剰なライブラリは導入しない。
* 保守性と拡張性を重視する。

---

# 今後相談したい内容

今後は以下のようなテーマについて段階的に設計・実装を進める予定。

* ディレクトリ構成の改善
* UI設計
* コンポーネント設計
* API設計
* Cloudflare Workers実装ccxz
* Vite環境構築
* CSS設計
* JavaScript設計
* データフロー
* パフォーマンス最適化
* リファクタリング
* 将来的なTypeScript移行
* 将来的な統計・期待値機能の追加設計


